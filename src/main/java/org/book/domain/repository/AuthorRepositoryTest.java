package org.book.domain.repository;

import static org.junit.Assert.assertNotNull;
import static org.junit.Assert.assertNull;
import static org.junit.Assert.fail;

import org.book.domain.model.Author;
import org.junit.Before;
import org.junit.Test;

public class AuthorRepositoryTest {

/*	@Before
	public void setUp() throws Exception {
		 Author auth1= new Author("Ramki", "10/10/1990","");
		 Author auth1= new Author("Pradeep", "11/11/1991","");
	        //save product, verify has ID value after save
	        assertNull(auth1.getId());
	        assertNull(auth2.getId());//null before save
	        this.userMongoRepository.save(user1);
	        this.userMongoRepository.save(user2);
	        assertNotNull(user1.getId());
	        assertNotNull(user2.getId());
	}

	@Test
	public void test() {
		fail("Not yet implemented");
	}

*/}
